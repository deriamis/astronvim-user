return {
    settings = {
        Lua = {
            runtime = { version = "LuaJIT" },
            diagnostics = { globals = { "astronvim", "vim" } },
            hint = { enable = true }
        }
    }
}
